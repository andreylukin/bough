// Package contract holds only tests: behaviour of the pinned
// unreal-agent harness that bough's engine relies on and the harness
// README does not promise (go/docs/unreal-engine.md §15.2). A pin bump
// that breaks one of them is a design review, not a test fix.
package contract
