{
  "name": "bough-web-e2e",
  "private": true,
  "version": "0.0.1",
  "description": "Playwright e2e tests for bough --web (sip terminal in the browser)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:model": "node --test 'model/**/*.test.ts'",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "@playwright/test": "1.62.1",
    "@types/node": "24.3.0",
    "typescript": "7.0.2"
  }
}
