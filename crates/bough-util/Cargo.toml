[package]
name = "bough-util"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }
dirs = { workspace = true }
# tokio: `time` only — with_timeout races the caller's future against a sleep on the
# caller's runtime. bough-util creates no runtime of its own.
tokio = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
tokio = { workspace = true }
