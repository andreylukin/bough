[package]
name = "bough-plugin-voice-instructions"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

# §10: HOW an agent answers, in ONE crate. A GLOBAL projection section for every agent (workers
# included), beside the write boundary: that block bounds what may be done, this one bounds how
# much is said.

[dependencies]
bough-kernel = { workspace = true }
async-trait = { workspace = true }
inventory = { workspace = true }
serde = { workspace = true }
schemars = { workspace = true }
bough-plugin-projection = { workspace = true }
