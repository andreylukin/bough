[package]
name = "bough-plugin-rollups-none"
autotests = false
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

# The `rollups` stub Provider and the Phase 4 SWAP subject (P4-D1). It satisfies the key and seals
# nothing: no model call, no appended step. In the catalog and in NO bundle — the `ledger-memory` /
# `projection-probe` / `tui-probe` precedent; the swap patch names it.

[dependencies]
bough-kernel = { workspace = true }
bough-plugin-ledger = { workspace = true }
bough-plugin-rollups = { workspace = true }
bough-plugin-commands = { workspace = true }
async-trait = { workspace = true }
inventory = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
schemars = { workspace = true }
chrono = { workspace = true }
anyhow = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }
bough-plugin-ledger-memory = { workspace = true }

[[test]]
name = "tests"
path = "tests/main.rs"
