[package]
name = "bough-plugin-fault-inject"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

# A CATALOG-ONLY row (decision D-C8, the `ledger-memory` / `agent-loop-scripted` precedent): in the
# binary, named by no bundle, mounted by a test's own `--patch`. It exists to break one named site
# on purpose so §17 Phase 8's failure-injection bullets are observable.

[dependencies]
bough-kernel = { workspace = true }
bough-util = { workspace = true }
bough-plugin-agents = { workspace = true }
bough-plugin-ledger = { workspace = true }
bough-plugin-projection = { workspace = true }
bough-plugin-tools = { workspace = true }
async-trait = { workspace = true }
inventory = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
schemars = { workspace = true }
chrono = { workspace = true }
parking_lot = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }
