[package]
name = "bough-plugin-schedule-manual"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

# §9: the TEST schedule Provider. In the catalog, in NO bundle: a test's or a script's own `--patch` mounts it, which is what makes the seam a swap subject and every downstream test clock-free.

[dependencies]
bough-kernel = { workspace = true }
bough-util = { workspace = true }
async-trait = { workspace = true }
inventory = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
schemars = { workspace = true }
chrono = { workspace = true }
thiserror = { workspace = true }
anyhow = { workspace = true }
parking_lot = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }
bough-plugin-schedule = { workspace = true }
