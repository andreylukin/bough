[package]
name = "bough-plugin-tool-tell"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

# §5: `tell` — mail a SIBLING lane. `inbox` reads mail and nothing wrote it: lanes could not talk
# to each other at all, which is how the curator sat idle while two personas told trunk to feed it.

[dependencies]
bough-kernel = { workspace = true }
bough-plugin-tools = { workspace = true }
bough-plugin-agents = { workspace = true }
bough-plugin-ledger = { workspace = true }
async-trait = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
schemars = { workspace = true }
inventory = { workspace = true }
chrono = { workspace = true }
uuid = { workspace = true }
