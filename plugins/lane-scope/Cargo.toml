[package]
name = "bough-plugin-lane-scope"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

# A CONSUMER: per-lane persona section and `tools.restrict` from config (§5). The second scope
# consumer after the leader set, and V6's subject — shadowing and restriction must work for an
# ORDINARY lane, not only for the leader. Row `lane.scope` in `bough-base`.

[dependencies]
bough-kernel = { workspace = true }
bough-plugin-ledger = { workspace = true }
bough-plugin-agents = { workspace = true }
bough-plugin-tools = { workspace = true }
bough-plugin-projection = { workspace = true }
async-trait = { workspace = true }
inventory = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
schemars = { workspace = true }
chrono = { workspace = true }
anyhow = { workspace = true }
tracing = { workspace = true }
parking_lot = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }
bough-plugin-ledger-memory = { workspace = true }
bough-plugin-projection-assembler = { workspace = true }
bough-plugin-workers = { workspace = true }
tempfile = { workspace = true }
