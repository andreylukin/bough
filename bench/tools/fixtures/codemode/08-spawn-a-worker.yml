# Recorded transcript: 08-spawn-a-worker, CODE-MODE arm.
#
# MERGE: the program passed a SECOND argument, `{name: "notes summary"}`, which `spawn_worker`'s
# schema binds to its `tools` property (`SpawnArgs = {task, tools?}`) — so the call was refused and
# the round spawned nothing. It had never been exercised: before `agent-loop` declared `workers`,
# every `spawn_worker` in the tree came back "workers seam unavailable" whatever it carried.
#
# The same task as `../typed/08-spawn-a-worker.yml`, in ONE `run(program)` round plus the answer. The token
# counts are deliberately larger per round (the surface section is in the prompt, the program is in
# the output) and fewer rounds: that trade is the thing the bench measures.
{
  "entries": {
    "llm.anthropic": {
      "plugin": "llm-replay",
      "config": {
        "strict": true,
        "models": "*",
        "rounds": [
          {
            "chunks": [
              {
                "type": "tool_call",
                "id": "c0",
                "name": "run",
                "input": {
                  "program": "const out = await agent(\"Read src/notes.md in the bench fixture repo and summarise it in one sentence.\");\nconsole.log(out);"
                }
              },
              {
                "type": "usage",
                "input_tokens": 5600,
                "output_tokens": 320
              },
              {
                "type": "end",
                "stop": "tool_use"
              }
            ]
          },
          {
            "chunks": [
              {
                "type": "text",
                "text": "src/notes.md says the marker is MARKER_ONE and nowhere else."
              },
              {
                "type": "usage",
                "input_tokens": 1800,
                "output_tokens": 60
              },
              {
                "type": "end",
                "stop": "end_turn"
              }
            ]
          },
          {
            "chunks": [
              {
                "type": "text",
                "text": "Spawned a worker to summarise src/notes.md."
              },
              {
                "type": "usage",
                "input_tokens": 6400,
                "output_tokens": 60
              },
              {
                "type": "end",
                "stop": "end_turn"
              }
            ]
          }
        ]
      }
    }
  }
}
