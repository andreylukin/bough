# Recorded transcript: 07-search-then-edit, CODE-MODE arm.
#
# The same task as `../typed/07-search-then-edit.yml`, in ONE `run(program)` round plus the answer. The token
# counts are deliberately larger per round (the surface section is in the prompt, the program is in
# the output) and fewer rounds: that trade is the thing the bench measures.
{
  "entries": {
    "llm.anthropic": {
      "plugin": "llm-replay",
      "config": {
        "strict": true,
        "models": "*",
        "rounds": [
          {
            "chunks": [
              {
                "type": "tool_call",
                "id": "c0",
                "name": "run",
                "input": {
                  "program": "console.log(await bash(\"rg -l MARKER_ONE src\", \"repo:search:marker\"));\nconsole.log(await view(\"src/notes.md\"));\nconsole.log(await patch(`[src/notes.md#]\nSWAP 3.=3:\n+the marker is MARKER_TWO here`));"
                }
              },
              {
                "type": "usage",
                "input_tokens": 5600,
                "output_tokens": 320
              },
              {
                "type": "end",
                "stop": "tool_use"
              }
            ]
          },
          {
            "chunks": [
              {
                "type": "text",
                "text": "src/notes.md was the only hit; it says MARKER_TWO now."
              },
              {
                "type": "usage",
                "input_tokens": 6400,
                "output_tokens": 60
              },
              {
                "type": "end",
                "stop": "end_turn"
              }
            ]
          }
        ]
      }
    }
  }
}
