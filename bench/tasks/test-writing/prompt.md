Write a unittest test file `test_slugify.py` for slugify.py. Cover at least: lowercasing, spaces becoming hyphens, a run of several punctuation characters collapsing to a single hyphen, leading/trailing separators being trimmed, and an input with no alphanumeric characters returning the empty string. The tests must pass (`python3 -m unittest`) and must be strict enough to catch subtly wrong implementations of the documented behavior. Do not modify slugify.py.
