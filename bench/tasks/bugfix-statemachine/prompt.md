The ticket workflow misbehaves: auto-close after repeated reopens never kicks in, and `python3 -m unittest` has a failing integration test showing it. The intended workflow rules are documented in machine.py's module docstring — the tests encode them. Find the root cause and fix the machine so its actual behavior matches every documented rule and all tests pass. Do not change test_ticket.py, dispatcher.py, or cli.py — the dispatcher's strict first-match-in-table-order contract is intended behavior. Make the smallest change that fixes the root cause.
