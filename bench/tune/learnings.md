# Tuner learnings (persists across campaigns; newest last)

- [2026-07-21] Night 1 (tune-2026-07-20): 7 challengers, all check-discipline
  variations. Six refuted outright. iterative-check-execution ("RUN your check
  after each change") won 35/42 vs 32/42 but FAILED n=6 confirmation (13/24 vs
  12/24) — the refactor-behavior-preserving gain was pure variance. Do not
  re-propose bare check-execution phrasing changes.
- [2026-07-21] refactor-behavior-preserving is a ~33% coin-flip for haiku
  (pooled baseline 3/9), NOT a hard failure. Movement on it in a single sweep
  is noise. Same caution applies to migrate-format (~44%).
- [2026-07-21] Live suggestive signal worth a targeted hypothesis:
  migrate-format pooled 8/9 with the check-execution rule vs 4/9 baseline.
  If targeting it, diagnose from its failed transcripts, don't just rephrase
  check discipline.
- [2026-07-21] Output-mismatch fail counts did NOT drop under any of the six
  check-discipline variants (5 baseline -> 4-8 across variants). The bottleneck
  for output-mismatch is elsewhere — read transcripts before theorizing.
- [2026-07-21] transform-three-phase-verify (mutate): Decompose transformational verification into three isolated phases (test logic on simple example, apply to full dataset, verify output matches spec) to catch errors early and extend check-execution discipline benefits for migration and format-conversion tasks. -> 4/4 vs champion 4/4; paired stable +0 noisy +0 (bar 2.0) -> not promoted; moved: nothing
- [2026-07-21] explicit-edit-verification (mutate): Require supervisor to show before-and-after code or explicit change confirmation when making edits. This catches silent failures and wrong fixes before full verification runs waste tokens on broken fixes. -> 36/42 vs champion 34/42; paired stable +1 noisy +1 (bar 2.0) -> not promoted; moved: fanout-heavy, feature-config-priority, feature-pipeline, migrate-format
- [2026-07-21] requirement-first-check (mutate): Agents in failing bugfix/feature/migrate tasks loop without reading requirements or establishing checks. Move the check-definition phase to require reading requirements FIRST and defining success criteria from requirements—not from code you're about to write—so agents understand their target before making changes. -> 50/70 vs champion 47/70; paired stable +3 noisy +0 (bar 2.0) -> PROMOTED; moved: fanout-bugs, rename-precise, bugfix-inventory, refactor-rename, fanout-heavy, feature-topwords, bugfix-statemachine, feature-pipeline
- [2026-07-21] requirement-first-check end-of-night confirmation: confirmation sweep failed -> REFUTED — night gain was variance
- [2026-07-21] online-nightly-2026-07-21-call-globals-directly-front-loaded (online): Programs crash and burn a whole round when the model tries to obtain host functions via import/require or the Node stdlib (`const { bash } = await import('node:util')` → 'already been declared'); front-loading the bare imperative CALL THEM DIRECTLY and citing the exact failing form makes a weak model use the globals as-is instead of acquiring them. -> QUEUED for bench race (evidence: session 83c295ec (import('node:util') → 'already been declared'); mechanism is the exact class the current rule targets but states last/buried.)
- [2026-07-22] online-nightly-2026-07-21-call-globals-directly-front-loaded (online): Programs crash and burn a whole round when the model tries to obtain host functions via import/require or the Node stdlib (`const { bash } = await import('node:util')` → 'already been declared'); front-loading the bare imperative CALL THEM DIRECTLY and citing the exact failing form makes a weak model use the globals as-is instead of acquiring them. -> 36/48 vs champion 40/48; paired stable -1 noisy -3 (bar 2.0) -> not promoted; moved: migrate-format, feature-pipeline, wiring-stats, fanout-eight, feature-config-priority, longhorizon-feature, fanout-bugs, refactor-behavior-preserving
- [2026-07-22] bugfix-reproduce-then-locate (mutate): Add a scoped gate in the work-loop section — on any cited failing test or described defect, RUN it and SEE the failure first, then change the exact rule/line at fault; a bugfix that changed no code fixed nothing — funded by tightening the byte-diff paragraph, so the agent stops declaring done after zero edits (as it did on bugfix-statemachine: read docstring, ran unittest once → 'OK', set done with no fix). -> 37/48 vs champion 40/48; paired stable -2 noisy -1 (bar 2.0) -> not promoted; moved: wiring-stats, fanout-eight, feature-config-priority, longhorizon-feature, refactor-behavior-preserving
- [2026-07-22] merge-call-directly-cite-form-champion-order (merge): Merge the partner's front-loaded imperative (`CALL THEM DIRECTLY`) and its exact failing-form citation (`import('node:util')`) into the champion's host-functions paragraph while keeping the champion's clause order and stdlib facts, so the weak model reads the do-this instruction before the don't-do-this list and recognizes the precise import form it tends to crash on. -> 39/48 vs champion 40/48; paired stable +0 noisy -1 (bar 2.0) -> not promoted; moved: bugfix-crossmodule, migrate-format, feature-pipeline, wiring-stats, fanout-eight, feature-config-priority, longhorizon-feature, refactor-behavior-preserving
- [2026-07-22] early-green-means-wrong-check (mutate): In the work-loop section, add a verb-tied structural gate — a fix/add/change request is not done until a file was edited AND the check passes on that edited workspace, and a check that passes before any edit is WRONG not satisfied — funded by consolidating the redundant end-with-text prose in Chat style; this attacks the exact bugfix-statemachine moment where `unittest | grep OK` passed on an unfixed workspace and the agent shipped zero edits. -> 37/48 vs champion 40/48; paired stable -3 noisy +0 (bar 2.0) -> not promoted; moved: migrate-format, fanout-heavy, feature-config-priority, longhorizon-feature, feature-topwords, refactor-behavior-preserving, rename-precise, bugfix-inventory
- [2026-07-22] compress-artifact-dilution-cut (mutate): Compress the ~1600-char artifact() wall-of-text in system.md to ~700 chars (preserving every fact: signature, once-per-file, out-of-workspace, quality bar, comment layer) — the bench model never builds artifacts, so this paragraph is pure prompt dilution whose removal frees the weak model's attention for the work-loop/check rules; this is 'deleting a rule the model ignores' rather than another refuted check-gate. -> 37/48 vs champion 40/48; paired stable +0 noisy -3 (bar 2.0) -> not promoted; moved: bugfix-crossmodule, migrate-format, feature-pipeline, wiring-stats, feature-config-priority, longhorizon-feature, refactor-behavior-preserving
- [2026-07-22] merge-globals-imperative-and-import-form (merge): Merge the partner's front-loaded imperative caps (`CALL THEM DIRECTLY`) and its exact failing-form citation (`import('node:util')`) into the champion's host-functions paragraph while preserving the champion's clause order and stdlib facts, so the weak model reads the do-this before the don't-do list and recognizes the precise import form it crashes on. -> 40/48 vs champion 40/48; paired stable +1 noisy -1 (bar 2.0) -> not promoted; moved: bugfix-crossmodule, feature-pipeline, longhorizon-feature, refactor-behavior-preserving
- [2026-07-22] test-mutation-red-then-revert (mutate): Add a scoped structural gate to the work-loop section — when the task is to WRITE or STRENGTHEN tests, after the suite is green the agent must inject one obvious defect into the code under test, confirm a test goes RED, then revert — funded by consolidating the duplicated end-with-text prose across 'Ending your turn' and 'Chat style'; this attacks mutant-not-caught, a failure mode (weak tests that pass on both correct and broken code) that no prior variant has touched, since every refuted attempt targeted check-discipline/edit-gates instead. -> 34/48 vs champion 40/48; paired stable -3 noisy -3 (bar 2.0) -> not promoted; moved: migrate-format, fanout-eight, longhorizon-feature, fanout-bugs, refactor-behavior-preserving
