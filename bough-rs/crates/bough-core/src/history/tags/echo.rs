//! Failure echo (port of `src/history/echo.ts`): the note + loop guard (fires
//! at exactly 3-in-2min-same-session; signature path catches the
//! 100-distinct-commands incident; success-prefix LIKE-escape).
//! STUB (wave 2, row 2.11).
