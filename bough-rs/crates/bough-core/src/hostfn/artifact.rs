//! `artifact()` host fn (port of `src/hostfn/artifact.ts`): publish a file
//! under the session's artifact dir (filesystem-backed, survives a DB reset),
//! traversal confined, returns `{url, href}`. STUB (wave 2, row 2.7).
