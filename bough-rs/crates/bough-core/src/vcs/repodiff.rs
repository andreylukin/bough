//! Changes rail git layer (port of `src/vcs/repodiff.ts`): the change set is
//! `git diff <base>` plus untracked files; a non-repo degrades, never fails;
//! noise filter; per-path revert posts a no-wake note. STUB (wave 2, row 2.14).
