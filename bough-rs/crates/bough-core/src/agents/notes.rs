//! System notes + the wake rule (port of `src/agents/notes.ts`). Note format
//! is verbatim product surface (the TUI parses it); a burst drains once; an
//! interrupted session stays stopped; `postSystemNote` to a missing session
//! answers `dropped`. STUB (wave 2, row 2.3).
