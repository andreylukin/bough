//! Subagent launch + result building + naming (port of
//! `src/agents/subagent.ts`). Isolation both directions (spawner sentinel
//! never in child LlmParams); 4-status outcome matrix; depth cap message
//! `depth limit (2)`. STUB (wave 2, row 2.2).
