The prompt section files (`*.md`) are vendored here verbatim from
`src/prompt/sections/` by the prompt porter (wave 1, row 1.24) and
`include_str!`-ed from `assemble.rs`. A missing section file is fatal at boot.
