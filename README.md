# bough (rebuild)

One harness: resident lane-agents in a TUI over a single append-only ledger. Everything is a
plugin except the center. `REQUIREMENTS.md` is the spec; `AGENTS.md` says how to work here;
`BUILD.md` is the phase ledger.
