{
  "compilerOptions": {
    "target": "ESNext",
    "module": "Preserve",
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "noEmit": true,
    "strict": true,
    "jsx": "react-jsx",
    "jsxImportSource": "@opentui/react",
    "types": ["bun-types"],
    "lib": ["ESNext", "DOM", "DOM.AsyncIterable"],
    "skipLibCheck": true,
    "allowJs": true,
    "verbatimModuleSyntax": false,
    "esModuleInterop": true,
    "resolveJsonModule": true
  },
  "// lib": "DOM.AsyncIterable is load-bearing: without it lib.dom's ReadableStream carries no [Symbol.asyncIterator], and `for await (const chunk of stream)` — which Bun supports and jobs.ts, mcp/client.ts and the echo fixture all use — does not typecheck.",
  "// exclude":"Agent worktrees under .claude are checkouts of other branches; they are not part of this build and must not be walked by check/test.",
  "exclude": [".claude", "node_modules"]
}
